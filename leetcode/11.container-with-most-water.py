# @leet start
class Solution:
    def maxArea(self, height: list[int]) -> int:
        pass


# @leet end

"""
 - volume = (j - i) * min(height[i], height[j])
 - probably want to start on either side and 'move inward'
 - compare close+tall vs far+short
 - 

"""
